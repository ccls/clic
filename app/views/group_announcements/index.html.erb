<% javascripts('simply_helpful') %>
<h2>Group Announcements:</h2>

<% if @announcements.length > 0 %>
 <table id='announcements'><thead>
 <tr><th>Title</th></tr></thead><tbody>
 <%= render :partial => 'announcement', :collection => @announcements %>
 </tbody></table><!-- id='announcements' -->
<% else %>
 <p>Sorry, but no announcements yet.</p>
<% end %>

<p>
 <%= link_to 'Group', 
   group_path(@group), :class => 'button' %>
 <%= link_to 'Create New Group Announcement', 
   new_group_announcement_path(@group), :class => 'button' %>
</p>
