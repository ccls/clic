<%= form_for(@profession) do |f| %>
 <%= f.error_messages %>
 <%= f.wrapped_text_field :name, :class => 'autosize' %>



name:string

required

Add "other" to list

Add "other_profession" string field to User model, required if "other" is selected.



 <p>
  <%= f.submit( ((@profession.new_record?)?'Create':'Update')<<" Profession" ) %>
  <% unless( @profession.new_record? ) %>
    <%= link_to 'Cancel and Show Profession', @profession, :class => 'button' %>
  <% end %>
  <%= link_to 'Cancel and Show All Professions', professions_path, :class => 'button' %>
 </p>
<% end %>
